{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@types/aws-lambda": "^8.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "~5.0.0",
    "esbuild": "~0.19.0"
  }
}
